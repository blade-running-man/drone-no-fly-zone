import { style } from '@vanilla-extract/css';

export const mapContainer = style({
  position: 'relative',
  height: '100%',
  width: '100%',
});
export const opacityController = style({
  position: 'absolute',
  padding: '10px',
  left: 0,
  bottom: 0,
  borderRadius: '0 5px 0 0',
  backgroundColor: '#fff',
});
